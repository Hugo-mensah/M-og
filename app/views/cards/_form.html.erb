<%= simple_form_for(card) do |form| %>
  <% if card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
        <% card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <%= form.input :pseudonym %>
  <%= form.input :image %>
  <%= form.input :name %>
  <%= form.input :surname %>
  <%= form.input :power %>
  <%= form.submit %>
<% end %>
